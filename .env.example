# Server-only. Never expose these to the client bundle.
# Copy to .env.local and fill in when those integrations are connected.
# Never commit real .env / .env.local files.

# PostgreSQL connection string. Leave empty for static fallback (build/CI/local).
# After setting it on the VPS, apply:
#   npm run db:migrate
DATABASE_URL=

TELEGRAM_BOT_TOKEN=
TELEGRAM_OWNER_ID=
CMS_PREVIEW_SECRET=
